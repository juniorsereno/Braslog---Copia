# Variáveis de ambiente específicas para Docker
# Copie este arquivo para .env e ajuste os valores conforme necessário

# Configurações da aplicação
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0

# Desabilitar telemetria do Next.js
NEXT_TELEMETRY_DISABLED=1

# Configurações do banco de dados
# Substitua pelos valores reais do seu projeto Supabase
DATABASE_URL="postgresql://postgres:[password]@[host]:[port]/[database]"

# Configurações do Supabase
# Obtenha estes valores no painel do Supabase
NEXT_PUBLIC_SUPABASE_URL="https://[project-id].supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="[sua-chave-anonima]"
SUPABASE_SERVICE_ROLE_KEY="[sua-chave-service-role]"

# Configurações de segurança (gere chaves seguras para produção)
NEXTAUTH_SECRET="[gere-uma-chave-secreta-segura]"
NEXTAUTH_URL="http://localhost:3000"

# Configurações de logging (opcional)
LOG_LEVEL=info

# Configurações de performance (opcional)
MAX_MEMORY=512
MAX_CPU=1